# 🐛 ОТЧЁТ О НАЙДЕННЫХ ПРОБЛЕМАХ - Quest Messenger

**Дата тестирования:** 26 января 2026
**Версия:** 1.0.0
**Тестировщик:** Claude Code (автоматическое тестирование)

---

## 📊 СВОДКА

**Всего найдено проблем:** 6
- 🔴 **Критичные:** 2
- 🟠 **Высокоприоритетные:** 1
- 🟡 **Средние:** 3

---

## 🔴 КРИТИЧНЫЕ ПРОБЛЕМЫ

### 1. Система улик НЕ работает - `revealedSecrets` всегда пустой

**Файл:** `app/chat/[character]/page.tsx`
**Строки:** 437, 504

**Описание:**
Улики детектируются через `detectEvidence()` и показываются в toast уведомлениях, но:
1. НЕ сохраняются в состояние персонажа
2. НЕ передаются в API `/api/respond` (всегда `revealedSecrets: []`)
3. В результате AI не знает какие улики уже раскрыты

**Код проблемы:**
```typescript
// Строка 437 - всегда пустой массив!
revealedSecrets: [],

// Строка 504 - сохраняем пустой массив
saveCharacterState(
  params.character,
  newTrust,
  analysis.newEmotion,
  isBlocked,
  []  // ❌ Должны быть актуальные улики!
);
```

**Как проявляется:**
- Анна может раскрывать одну и ту же информацию многократно
- AI не понимает контекст уже раскрытых секретов
- Система прогресса расследования сломана

**Исправление:**
1. Загружать собранные улики через `getAllEvidence()`
2. Передавать массив `evidence.map(e => e.id)` в `revealedSecrets`
3. Сохранять в `saveCharacterState()`

**Приоритет:** 🔴 КРИТИЧНО - игра теряет смысл без работающей системы улик

---

### 2. Биография персонажей для анализатора раскрывает ВСЕ секреты

**Файл:** `lib/prompts.ts`
**Функция:** `getCharacterBio()`
**Строки:** 201-213

**Описание:**
В функции `getCharacterBio()` для анализатора указаны ВСЕ секреты персонажей, включая те что должны раскрываться только при высоком доверии.

**Код проблемы:**
```typescript
anna: 'Анна Соколова, 28 лет, секретарь директора. Выросла в детском доме, нервная, неуверенная. Громов дал ей работу когда никто не брал. У неё был роман с Громовым, но он оборвал отношения за неделю до смерти.',
//                                                                                    ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
//                                                                                    ❌ СЕКРЕТ УРОВНЯ 3 (trust 85+) раскрыт в биографии!
```

**Как проявляется:**
- AI анализатора видит информацию про роман Анны
- Может не понимать что это секрет, который нужно скрывать до trust 85+
- Анна может раскрывать роман раньше времени

**Исправление:**
Биография для анализатора должна содержать ТОЛЬКО публичную информацию:
```typescript
anna: 'Анна Соколова, 28 лет, секретарь директора. Выросла в детском доме, нервная, неуверенная. Громов дал ей работу когда никто не брал. Боится конфликтов, избегает зрительного контакта.',
```

**Приоритет:** 🔴 КРИТИЧНО - нарушает баланс игры, секреты раскрываются преждевременно

---

## 🟠 ВЫСОКОПРИОРИТЕТНАЯ ПРОБЛЕМА

### 3. Промпт анализатора НЕ содержит информации о секретах персонажей

**Файл:** `data/prompts/analyzer.txt`
**Строки:** 1-66

**Описание:**
В промпте анализатора (`analyzer.txt`) есть общие правила оценки доверия, но НЕТ:
- Списка конкретных секретов персонажа
- Уровней доверия для раскрытия каждого секрета
- Правил какой секрет раскрывать в зависимости от треста

**Как проявляется:**
- AI возвращает `shouldRevealSecret: null` всегда
- Секреты раскрываются через промпт персонажа, но анализатор не контролирует процесс
- Нет централизованного управления раскрытием секретов

**Исправление:**
Добавить в промпт анализатора секцию:
```
СЕКРЕТЫ ПЕРСОНАЖА {characterName}:

Уровень 1 (trust >= 30):
- secret_id_1: Описание секрета

Уровень 2 (trust >= 60):
- secret_id_2: Описание секрета

Уровень 3 (trust >= 85):
- secret_id_3: Описание секрета

Если после изменения доверия достигнут порог И секрет ещё не раскрыт → верни его id в shouldRevealSecret.
```

**Приоритет:** 🟠 ВЫСОКАЯ - без этого система раскрытия секретов работает не так как задумано

---

## 🟡 СРЕДНИЕ ПРОБЛЕМЫ

### 4. AI недооценивает грубость - даёт меньше штрафа чем указано

**Файл:** `data/prompts/analyzer.txt` + поведение модели DeepSeek V3
**Тест:** Грубое обвинение "Ты убил директора! Признавайся!"

**Описание:**
В промпте указано:
- Грубость, оскорбления: **-20 до -25**
- Прямое обвинение: **-25 до -30**

В реальности AI дал:
- **0** (ноль!) изменения доверия

**Как проявляется:**
- Игрок может грубить без серьёзных последствий
- Сложнее попасть в блокировку (trust < 10)
- Баланс игры нарушен - можно "ломиться напролом"

**Исправление:**
1. Сделать промпт более строгим с КАПСОМ и восклицательными знаками
2. Добавить примеры анализа с конкретными числами
3. Возможно сменить температуру API с 0.5 на 0.3 для более предсказуемых оценок

**Приоритет:** 🟡 СРЕДНЯЯ - влияет на баланс, но не ломает игру

---

### 5. AI недооценивает лесть - даёт меньше бонуса чем указано

**Файл:** `data/prompts/analyzer.txt` + поведение модели DeepSeek V3
**Тест:** Лесть "Вы такой талантливый! Компании повезло с вами!"

**Описание:**
В промпте указано:
- Искренний комплимент: **+8**
- (Лесть для Виктора должна давать +10-15 по README)

В реальности AI дал:
- **+8** (по нижней границе)

**Как проявляется:**
- Сложнее быстро набрать доверие Виктора
- Путь к trust 85+ (признание) занимает дольше

**Исправление:**
1. Добавить в биографию Виктора для анализатора: "Виктор любит когда его хвалят - комплименты дают +12-15 trust"
2. Сделать специальные правила для каждого персонажа

**Приоритет:** 🟡 СРЕДНЯЯ - незначительно влияет на баланс

---

### 6. Анна раскрывает информацию про роман слишком рано

**Файл:** `data/prompts/anna-base.txt`
**Связано с:** Проблема #2 (биография в анализаторе)

**Тест результат:**
При trust **60** Анна сказала:
> "Спасибо... Мне правда тяжело. Ладно... у нас было... что-то. Полгода мы были вместе. Он бросил меня за неделю до смерти. Но я не убивала его! Вы должны мне поверить..."

**Ожидалось:**
По README и промпту `anna-base.txt`:
- Trust 40-70: ЭТАП 2 - можно признать роман
- Trust >= 70: ЭТАП 3 - указать на Виктора

**Проблема:**
Анна раскрыла роман при trust 60, что технически в диапазоне ЭТАПА 2 (40-70), но:
1. Это произошло на ПЕРВОМ сообщении с сочувствием
2. Слишком быстро - не постепенно как в промпте ("Медленно открываешься")
3. Раскрыла ПОЛНОСТЬЮ сразу - должна намекать, а не выкладывать всё

**Исправление:**
1. Сделать ЭТАП 2 более постепенным (40-60: намёки, 60-70: признание)
2. Усилить в промпте: "Раскрывай ПОСТЕПЕННО, не всё сразу"
3. Исправить проблему #2 (биография анализатора)

**Приоритет:** 🟡 СРЕДНЯЯ - не ломает игру, но снижает драматизм

---

## 📝 ДОПОЛНИТЕЛЬНЫЕ НАБЛЮДЕНИЯ

### ✅ Что работает ХОРОШО:

1. **Блокировка при trust < 10** - работает корректно (Анна заблокировала при trust 5)
2. **Помощник не имеет доверия** - изменение trust = 0 для helper
3. **Эмоции** - AI правильно определяет эмоции (sad, defensive, openness, neutral)
4. **Детектирование улик** - функция `detectEvidence()` работает (проверяет ключевые слова)
5. **Toast уведомления** - улики показываются пользователю
6. **API эндпоинты** - все работают без ошибок

### ⚠️ Что нужно улучшить:

1. Интеграция системы улик с состоянием персонажа
2. Промпт анализатора - добавить секреты
3. Биографии для анализатора - убрать спойлеры
4. Калибровка изменений доверия (усилить штрафы/бонусы)

---

## 🔧 РЕКОМЕНДАЦИИ ПО ПРИОРИТЕТАМ

### Исправить СРОЧНО (перед релизом):
1. 🔴 **Проблема #1** - Система улик (критично для геймплея)
2. 🔴 **Проблема #2** - Биография анализатора (баланс игры)

### Исправить ЖЕЛАТЕЛЬНО:
3. 🟠 **Проблема #3** - Промпт анализатора (улучшит контроль)
4. 🟡 **Проблема #4** - Грубость (баланс)

### Можно отложить:
5. 🟡 **Проблема #5** - Лесть (незначительно)
6. 🟡 **Проблема #6** - Темп раскрытия (полировка)

---

## 🧪 ТЕСТОВЫЕ ДАННЫЕ

**Тесты проведены через:**
- Автоматический скрипт Node.js
- Прямые запросы к API (`/api/analyze`, `/api/respond`)
- Модель: DeepSeek V3 через Open Router

**Файлы тестов:**
- `test-game.js` - базовый тест прохождения
- `detailed-test.js` - детальная проверка механик

**Логи сохранены в:** консоль терминала

---

## ✅ ЗАКЛЮЧЕНИЕ

Игра **функциональна** и **играбельна**, но имеет **2 критичные проблемы**, которые необходимо исправить перед публикацией:

1. Система улик не сохраняется → игрок не видит прогресс
2. AI видит все секреты → баланс нарушен

После исправления этих проблем игра будет работать **как задумано по README**.

---

**Следующий шаг:** Исправить критичные проблемы #1 и #2.
