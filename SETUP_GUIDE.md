# 🎯 Полная инструкция по запуску через терминал

**Версия:** 0.6.0
**Дата:** 23 января 2026
**Для:** Windows, Mac, Linux

---

## Часть 1: Подготовка (первый раз)

### Шаг 1.1: Проверить Node.js

Откройте терминал (PowerShell на Windows) и напишите:

```bash
node --version
npm --version
```

**Ожидаемый результат:**
```
v18.17.1
9.8.1
```

**Если ошибка "node не найден":**
1. Скачайте Node.js с https://nodejs.org
2. Установите (выберите "Recommended")
3. Перезагрузите терминал
4. Повторите проверку

---

### Шаг 1.2: Перейти в папку проекта

**На Windows:**
```bash
cd C:\Users\Гриша\Desktop\Messengerquest
```

**На Mac:**
```bash
cd ~/Desktop/Messengerquest
```

**На Linux:**
```bash
cd ~/Desktop/Messengerquest
```

**Проверить что вы в правильной папке:**
```bash
ls  # Mac/Linux
dir # Windows

# Должны увидеть: README.md, package.json, app/, components/ итд
```

---

### Шаг 1.3: Получить Open Router API ключ

1. Откройте браузер
2. Перейдите на https://openrouter.ai/keys
3. Нажмите **"Create Key"**
4. Скопируйте ключ (выглядит как `sk-or-v1-xxxx...`)
5. Сохраните в текстовый файл (пригодится далее)

---

### Шаг 1.4: Создать .env.local файл

**Вариант 1: Использовать пример**
```bash
cp .env.example .env.local
```

**Вариант 2: Создать вручную**

На Windows (PowerShell):
```bash
New-Item .env.local
```

На Mac/Linux:
```bash
touch .env.local
```

---

### Шаг 1.5: Добавить API ключ

Откройте файл `.env.local` в редакторе (Notepad, VS Code, итд) и добавьте:

```
OPENROUTER_API_KEY=sk-or-v1-xxxxxxxxxxxxxxxxxxxxxxxx
```

Замените `xxx...` на ваш ключ с шага 1.3

**Сохраните файл** (Ctrl+S)

---

### Шаг 1.6: Установить зависимости

В терминале (вы всё ещё в папке Messengerquest):

```bash
npm install
```

**Это займёт ~2-3 минуты.** Вы увидите много логов — это нормально.

**Когда закончится, вы увидите:**
```
added 300+ packages in 2m 45s
```

---

## Часть 2: Запуск (каждый раз)

### Способ 1: Быстрый старт

```bash
# 1. Перейти в папку (если вы её закрыли)
cd ~/Desktop/Messengerquest

# 2. Запустить dev-сервер
npm run dev

# 3. Открыть в браузере
# http://localhost:3000
```

### Способ 2: Пошаговый старт

```bash
# Шаг 1: Перейти в папку
cd ~/Desktop/Messengerquest

# Шаг 2: Проверить что всё установлено
npm list next

# Шаг 3: Очистить кэш (если были проблемы)
npm cache clean --force

# Шаг 4: Запустить dev-сервер
npm run dev

# Шаг 5: Найти в терминале строку типа:
# ✓ Ready in 2.5s
# Local:  http://localhost:3000
```

### Способ 3: Запуск в production

```bash
# 1. Собрать оптимизированную версию
npm run build

# 2. Запустить production сервер
npm start

# 3. Открыть в браузере
# http://localhost:3000
```

---

## Часть 3: Остановка и перезагрузка

### Остановить сервер

В терминале нажмите:
```
Ctrl + C
```

Вы увидите:
```
✓ Stopped
```

### Перезагрузить сервер

```bash
# 1. Нажмите Ctrl+C (остановить)
# 2. Затем запустите снова
npm run dev
```

---

## Часть 4: Проверка и диагностика

### Проверить что всё работает

```bash
# Проверить Node.js версию
node --version

# Проверить npm версию
npm --version

# Проверить что в папке проекта
ls package.json

# Проверить что .env.local создан
ls .env.local

# Проверить что API ключ добавлен
cat .env.local | grep OPENROUTER_API_KEY
```

### Если сервер не запускается

**Ошибка: "Port 3000 is already in use"**
```bash
# Запустить на другом порту
npm run dev -- -p 3001

# Открыть http://localhost:3001
```

**Ошибка: "API key not provided"**
```bash
# Проверить .env.local
cat .env.local

# Должно быть:
# OPENROUTER_API_KEY=sk-or-v1-xxx...

# Если пусто - добавить ключ и перезагрузить сервер
```

**Ошибка: "Module not found"**
```bash
# Переустановить зависимости
npm install

# Если не помогло:
rm -rf node_modules package-lock.json
npm install
```

---

## Часть 5: Полезные команды

```bash
# Запуск (основное)
npm run dev                 # Dev с hot reload
npm run build              # Собрать production
npm start                  # Запустить production

# База данных (опционально)
npm run db:init            # Инициализировать БД
npm run db:reset           # Сбросить БД

# Проверка
npm run lint               # Проверить типы и стиль
npm run build -- --dry-run # Проверить сборку

# Очистка (если проблемы)
npm cache clean --force    # Очистить npm кэш
rm -rf node_modules        # Удалить зависимости
npm install                # Переустановить
```

---

## Часть 6: Работа с браузером

### После запуска

Вы увидите в терминале:
```
✓ Ready in 2.5s
Local:  http://localhost:3000
```

**Нажмите на ссылку или:**
1. Откройте браузер
2. В адресной строке напишите: `http://localhost:3000`
3. Нажмите Enter

### Если не открывается

**Проверьте:**
1. Сервер запущен? (в терминале должны быть логи)
2. Правильный адрес? (http://localhost:3000 или :3001)
3. Интернет работает? (для API запросов)

### Горячая перезагрузка (Hot Reload)

При запуске `npm run dev`:
- Измените файл → автоматически перезагрузится в браузере
- Не нужно перезагружать руками!
- Работает для всего: компоненты, стили, бизнес-логика

---

## Часть 7: Тестирование

### Быстрый тест

```bash
# 1. Запустить сервер
npm run dev

# 2. Открыть http://localhost:3000

# 3. Выбрать персонажа (Виктор)

# 4. Написать: "Привет!"

# 5. Ждать ответ
```

**Ожидаемый результат:**
- ✅ Сервер запустился без ошибок
- ✅ Страница загрузилась
- ✅ Персонаж ответил с эмоджи

### Проверка консоли браузера

1. Нажмите **F12** (откроется DevTools)
2. Вкладка **Console**
3. Нажмите Enter в чате
4. Вы должны увидеть логи:
   ```
   [AI Analysis] ...
   [AI Response] ...
   ```

**Если красные ошибки:**
- Проверьте API ключ
- Проверьте интернет
- Смотрите Terminal (где запущен npm run dev)

---

## Часть 8: Частые проблемы и решения

| Проблема | Решение |
|----------|---------|
| Port 3000 занят | `npm run dev -- -p 3001` |
| API ключ не работает | Проверьте в .env.local и перезагрузите |
| Модуль не найден | `npm install` |
| Сервер не запускается | Проверьте Node.js версию (18+) |
| Медленно загружается | Ждите (первый раз может быть 30 сек) |
| Персонаж не отвечает | Проверьте интернет и консоль (F12) |

---

## Часть 9: Развертывание (Vercel)

**Опционально! Можно делать позже.**

```bash
# 1. Установить Vercel CLI
npm i -g vercel

# 2. Залогиниться
vercel login

# 3. Развернуть
vercel --prod

# 4. Добавить environment variables в Vercel
# - OPENROUTER_API_KEY
```

---

## 📝 Чек-лист

Перед первым запуском:
- [ ] Node.js 18+ установлен (`node --version`)
- [ ] npm установлен (`npm --version`)
- [ ] Вы в папке Messengerquest (`ls package.json`)
- [ ] .env.local создан (`ls .env.local`)
- [ ] API ключ добавлен в .env.local
- [ ] `npm install` выполнен
- [ ] `npm run dev` успешно запустился
- [ ] http://localhost:3000 открывается в браузере

---

**Версия:** 0.6.0
**Дата:** 23 января 2026
**Статус:** ✅ Production Ready

**Готовы к запуску!** 🚀
